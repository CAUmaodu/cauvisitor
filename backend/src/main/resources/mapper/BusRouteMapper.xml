<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.vistorservice.dao.BusRouteMapper">
    <select id="findAll" resultType="com.campus.vistorservice.model.BusRoute">
        SELECT id, name, 
               ST_AsGeoJSON(geom) as geometry
        FROM cau_gongjiao
    </select>
</mapper>
